#!/bin/bash

# Useful formulae from brew
brew install tree
brew install htop
brew install screenfetch
brew install wget
brew install coreutils # in order to use `gdate` command in `remind` script
brew install the_silver_searcher # in order to use `ag` command in `search` script
brew install MonitorControl # https://github.com/MonitorControl/MonitorControl